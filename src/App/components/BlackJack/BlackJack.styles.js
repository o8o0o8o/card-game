import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  button: {
    position: "relative",
  },
  playerHand: {
    position: "relative",
    top: 100,
  },
  chipHolder: {
    position: "relative",
    top: 300,
  },
});
