import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  base: {
    position: "absolute",
    top: 0,
    width: "120px",
    height: "180px",
    borderRadius: "15%",
    background: "white",
    border: "1px solid black",
  },
});
